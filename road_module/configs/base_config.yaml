project:
  name: road_extraction
  version: 1.0

paths:
  dataset: dataset/clean_road
  outputs: outputs/
  models: models/

training:
  batch_size: 4
  lr: 0.0001
  epochs: 50

model:
  architecture: Unet
  encoder: resnet34
